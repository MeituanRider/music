<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aftvc.top.dao.AdminMapper">
    <select id="veritypasswd" resultType="Integer">
    SELECT count(*) FROM admins where name = #{name} and password = #{password} ;
    </select>
</mapper>
